// 원시 타입 자료형 - Null
// 존재 X, 알 수 없는 값

// 다른 언어에서 Null은 하나의 값으로 활용
// JS는 오직 Null값만을 포함하는 '자료형'

let num = null;
console.log(typeof num); // JS의 오래된 오류 | 출력 : object
console.log(num === null); // === : 비교 연산자 | 출력: true
